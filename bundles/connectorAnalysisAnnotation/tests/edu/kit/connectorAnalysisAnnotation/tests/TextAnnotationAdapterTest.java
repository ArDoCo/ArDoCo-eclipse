import static org.junit.Assert.assertTrue;

public class TextAnnotationAdapterTest {
	
	@Test
	public void testAdapter() {
		TextAnnotationAdapter adapter = new TextAnnotationAdapter();
		assertTrue(False);
	}
}